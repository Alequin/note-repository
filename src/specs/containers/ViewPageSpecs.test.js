import assert from "assert"
import ViewPage from "./../../containers/viewPage/ViewPage.jsx"
import {mockFullNote1} from "./../../dev/fakeSummaryNotesData.js"

describe("View page", function(){

  let page

  beforeEach(() => {
    page = new ViewPage(mockFullNote1)
  })

  it("can", () => {

  })
})
